#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
/*Preprocessor commands*/
string commandString;
printf("enter command here:");
scanf(%d,commandString);
int numOfChars;
numOfChars=sizeof(commandString);
typedef struct{;
	/*enter more variables here*/
	char command[numOfChars];
};
